# NutriStock 開発メモ

## 公開先

**本番: GitHub Pages版**（2026-09-12〜、正式に本番採用）: https://kaeru510.github.io/nutristock/
- ソース: `index.html`（このフォルダ、gitリポジトリ）。リポジトリ: https://github.com/kaeru510/nutristock （個人用アカウント。研究室のGitHubアカウントとは意図的に分離）
- iframeに埋め込まれないトップレベルページなので、**カメラの権限問題が解消される**（バーコードスキャンがスマホでも動く）
- 一方で `db`/`sample` は使えない（`window.claude` が存在しないため）。**データはブラウザごとのlocalStorageのみ**（端末間の自動同期なし）。カメラの安定動作を優先し、意図的にこのトレードオフを選んだ
- デバッグ用: URLに `?debug=1` を付けると、画面上にコンソール(Eruda)が出せる／バーコード読み取り成功時に「📤 画像を共有」ボタンが数秒表示され、その場のカメラ映像をiOSの共有シート経由で保存・送信できる

**廃止: Artifact版**（claude.ai経由）: https://claude.ai/code/artifact/c3e95274-3a59-4b08-98cd-92ffbac7a7ad
- 2026-09-12以降、更新を終了。開くと「GitHub Pages版へどうぞ」という案内ページだけが表示される（削除機能が無いため上書きで対応）
- それ以前は`db`（クラウド保存）・`sample`（AI解析）が使える構成だったが、スマホでのカメラ権限問題が解決できず、GitHub Pages版に一本化した

更新の流れ（GitHub Pages版）: `index.html` を編集 → `cd myfolder/NutriStock && git add index.html && git commit -m "..." && git push`（リモート: `https://github.com/kaeru510/nutristock.git`、ローカルのgit identityはこのリポジトリだけ `kaeru510` / `kaeru510@users.noreply.github.com` に設定済み）。反映まで1〜2分。

## できること / できないこと

| 機能 | 状態 | 備考 |
|---|---|---|
| パントリー管理（賞味期限順・バッジ表示） | ✅ 動作 | |
| 栄養ダッシュボード（自動目標計算・メーター） | ✅ 動作 | Mifflin-St Jeor式ベースの概算。たんぱく質は目的別プリセット選択。食材換算表示は2026-09-15に削除（ユーザー判断で不要と判断） |
| 期限×不足栄養素の献立提案 | ✅ 動作 | Gemini APIキー未設定時のフォールバック。タンパク源＋野菜の組み合わせ提案ロジック |
| AIによる献立提案（今夜のおすすめ） | ✅ 動作（要APIキー） | 2026-09-15追加。詳細は下記「AI献立提案（ホーム）」 |
| お気に入り献立の保存 | ✅ 動作 | 2026-09-18追加。AI提案・食事記録を経由せずワンタップで再記録できる |
| レシピ表示（Google検索連携） | ✅ 動作（要APIキー） | 2026-09-19追加。実在レシピサイトを検索し出典を明記、無ければAI作成と明記。詳細は下記 |
| 献立の画像生成（Pollinations.ai） | ✅ 動作（APIキー不要・設定でON/OFF） | 2026-09-18追加。無料・デフォルトOFF・ボタン押下時のみ生成。詳細は下記 |
| 期限切れ通知（iOSショートカット連携） | ✅ 動作（要ショートカット設定） | 2026-09-18追加。歩数連携と同じ仕組み。詳細は下記 |
| 食事記録（手入力） | ✅ 動作 | |
| 歩数の記録 | ✅ 動作（iOS限定・要ショートカット設定） | 2026-09-16、手入力UIを廃止しiOSショートカット連携のみに一本化。詳細は下記「歩数の自動入力」 |
| バーコードスキャン（JANコード読み取り） | ✅ 動作（縦向き推奨） | 詳細は下記「バーコードスキャン実装メモ」 |
| 買い物リスト・期限切れ分離・カテゴリ検索・最近使った食材 | ✅ 動作 | 2026-09-12追加 |
| 在庫が残りわずかになったら買い物リストに自動追加 | ✅ 動作 | 2026-09-18追加。個数系の単位（g/ml以外）が残り1個になった時点で自動追加（詳細は下記） |
| パントリー健康度スコア | ✅ 動作 | 2026-09-18追加。AI不要のクライアント計算のみ。鮮度＋栄養バランスの単純合成スコア |
| レシートから一括パントリー登録 | ✅ 動作（要APIキー） | 2026-09-18追加。詳細は下記「レシート一括登録」 |
| 音声で食事記録 | ✅ 動作（要APIキー・対応ブラウザのみ） | 2026-09-18追加。iOS Safari 14.5+含め対応。詳細は下記「音声で食事記録」 |
| 体重ログ＋目標の自動再計算 | ✅ 動作 | 2026-09-18追加。AI不要。詳細は下記「体重ログ」 |
| バーコード→商品名の自動学習 | ✅ 動作 | 商品DBには接続不可なので、一度手入力すれば次回から自動入力される方式 |
| バーコード→Open Food Facts栄養情報の自動取得 | ✅ 動作 | 2026-09-18追加。無料・APIキー不要の外部DB。ヒットすれば栄養価も自動入力され、食事記録の選択肢にも出てくる。詳細は下記 |
| ダークモード手動切替 | ✅ 動作 | 2026-09-18追加。ヘッダーの🌙/☀️ボタン。CSS自体は元々`data-theme`属性に対応していたが切替UIが無かった |
| ホーム画面アイコン（PWA体裁） | ✅ 動作 | 2026-09-18追加。`manifest.json`・`apple-touch-icon.png`等を新規作成。詳細は下記 |
| 買い物リストの共有 | ✅ 動作（対応ブラウザのみ） | 2026-09-18追加。Web Share API。iOS Safari等の共有シートから他アプリへ送れる |
| 保存時のトースト通知 | ✅ 動作 | 「✓ 保存しました」を画面下に表示 |
| AI写真解析（食事記録） | ✅ 動作（要APIキー） | 2026-09-14〜。ユーザー自身のGemini APIキーを設定タブに登録すると使える。詳細は下記「AI写真解析（Gemini API直叩き）」 |
| バーコード/カスタム食材のAIアイコン推測 | ✅ 動作（要APIキー） | 2026-09-15追加。詳細は下記「AIアイコン推測」 |
| Apple ヘルスケア連携（完全自動） | ❌ 不可能 | 原理的にWeb版からは到達不可。歩数についてはiOSショートカット経由の準自動連携で代替済み（上記「歩数の記録」の行を参照） |

## AI写真解析（Gemini API直叩き）2026-09-14実装

GitHub Pages版には`window.claude`が存在せず`claude.use("sample")`が使えないため（旧Artifact版の`sample` capabilityはアカウント側制限で結局使えないままだった）、**ユーザー自身のGemini APIキーをブラウザから直接Google側APIに送る「Bring Your Own Key」方式**で実装。当初Claude APIで実装したが、Gemini APIには無料枠があり実質無料で使える見込みが高いことから、ユーザーの希望でGeminiに切り替えた（Claude版の実装は2026-09-14中に置き換え・廃止）。

- 設定タブに「AI設定（Gemini APIキー）」カードを追加。入力したキーは `localStorage`（`nutristock_geminiApiKey`）にのみ保存。`state`オブジェクトやdb同期の対象には含めない（他人と共有される経路が一切ないようにするため）
- ブラウザから直接 `https://generativelanguage.googleapis.com/v1beta/models/{model}:generateContent` を叩く。実機で確認したところ`Access-Control-Allow-Origin`がリクエスト元オリジンをそのまま返しており、特別なヘッダー無しでブラウザから直接fetchできる（Claude APIの`anthropic-dangerous-direct-browser-access`ヘッダーのような opt-in は不要）
- 新しい「Interactions API」(`/v1beta/interactions`)も検討したが、実機で叩いたところエラー応答が配列でラップされて返るなど公式ドキュメントの記載と食い違う挙動があったため、枯れていて挙動が安定している従来の`generateContent`エンドポイントを採用（`candidates[0].content.parts[].text`で応答テキストを取得、リクエストの画像は`contents[].parts[].inlineData:{mimeType,data}`）
- モデルは `gemini-3.5-flash-lite`（2026-09-14、`gemini-3.8-flash`で503過負荷が頻発したため軽量モデルに変更・画像入力対応は公式ドキュメントで確認済み）。画像はCanvasで長辺1024pxに縮小してから`toDataURL("image/jpeg",0.82)`でbase64化して送信（通信量・料金を抑えるため）
- プロンプトで「JSON配列のみで回答」と指示し、応答テキストからコードブロック記法(```)を除去してから`JSON.parse`。構造化出力の保証はないため、パース失敗時は`invalid_json`エラーとして扱う
- エラー判定は実機で確認した実際のエラー形状に基づく：無効なAPIキーは`HTTP 400`＋`error.details[0].reason === "API_KEY_INVALID"`で返る（`401`ではない）。レート制限は`error.status === "RESOURCE_EXHAUSTED"`
- `503`（サーバー過負荷）がGemini無料枠では頻繁に発生することを実運用で確認。1.5秒待って1回だけ自動リトライし、それでも失敗したら「Gemini側の一時的な混雑です」という専用メッセージを表示（通信環境の問題という誤解を避けるため、他のエラーとメッセージを分けた）
- さらに実機のDevTools Networkタブで確認したところ、503が返るまでに1分以上かかるケースがあった（即座にエラーが返るわけではない）。放置すると「解析中…」のまま数分固まって見えるため、各試行に`AbortController`で25秒のタイムアウトを設定。タイムアウトした場合もserver_overloadedとして扱う
- **2026-09-14〜15の実運用で判明**: 503連発は「サーバー過負荷」というより無料枠のレート制限（1分/1日あたりのリクエスト数上限）を使い切っていたのが実態だった可能性が高い。Geminiの無料枠は**モデルごとに別々の上限**を持つため、`gemini-3.8-flash`で上限に達した状態でも`gemini-3.5-flash-lite`に切り替えれば別枠として動く。時間を置く（分・日単位でリセットされる）のも有効。今後また「急にエラーが出るようになった」ときはまずこれを疑う
- **モデル選択機能（2026-09-15追加）**: 設定タブのAPIキーフォーム内に使用モデルの選択欄を追加（`gemini-3.5-flash-lite`/`gemini-3.1-flash-lite`/`gemini-3.5-flash`/`gemini-3.8-flash`）。`localStorage`（`nutristock_geminiModel`）に保存し、503が頻発するモデルから他モデルへその場で切り替えられるようにした
- **利用状況の可視化（2026-09-15追加、制約あり）**: GoogleのAPIキーには「残り使用量・上限」を取得できる公式エンドポイントが存在しない（Google AI Studioへのログインが必要な情報で、単なるAPIキーでは取得不可。`shared/rate-limits`ドキュメントで確認済み）。そのためNutriStock側で送ったリクエストの成功/失敗・時刻・モデルを`localStorage`（`nutristock_geminiUsageLog`、直近200件）に自前で記録し、設定タブに「本日の呼び出し回数・成功/失敗内訳・直近10件」を表示する形にした。これはGoogle公式の正確な残数ではない旨を画面上に明記し、正式な数値を確認したい場合のリンク（`https://aistudio.google.com/rate-limit?timeRange=last-28-days`）も併記した
- **重要**: Gemini Advanced等のチャット向けサブスクリプションとAPI利用は別物。Google AI Studio（aistudio.google.com/apikey）で別途APIキー発行が必要（無料枠あり、超過分のみ課金。設定カードの説明文にもその旨を明記）
- セキュリティ: `kaeru510/nutristock`はPublicリポジトリなので、APIキーは**絶対にコード・gitにコミットしない**。ユーザー本人が使う端末のブラウザに閉じたローカル保存のみ。共有端末では開発者ツールからキーを読み取れる点は設定画面に注意書き済み

### AI写真解析とパントリー在庫の連携（2026-09-14追加）

写真から検出した食材と、パントリー在庫の食材名は表記が一致しないことが多い（AIは「豚肉（肩ロース・炒め）」、パントリーは「豚こま切れ肉」等）ため、文字列の完全一致では紐づけできない。単純なあいまい一致ロジックを自前で書くより、**同じAI呼び出しの中でマッチングもAIにやらせる**方式を採用：

- 写真解析のプロンプトに、現在のパントリー在庫リスト（`id`と`name`のみ、JSON形式）を埋め込み、「検出した食品が在庫のどれかを使って作られたものだと合理的に推測できる場合のみ、そのidを`matchedPantryId`に入れる。自信が無ければnull」と指示。無理にマッチさせないよう明記（外食・総菜等は基本nullになるはず）
- AIが返した`matchedPantryId`は、実在するパントリーidの一覧と照合してから採用（AIが古い/存在しないidを返す可能性への防御。ハルシネーション対策）
- 解析結果の各行に「在庫と紐づけ」ドロップダウンを追加し、AIの推測をデフォルト選択にしつつ、ユーザーが手動で変更・解除（「紐づけない」）できるようにした。**AIの判定を鵜呑みにせず必ず人間が最終確認する**設計
- 「選択した項目を追加」時、紐づけが設定されている行は`deductPantryForConsumption()`（食事記録の手動フォームと共通のロジック）でパントリー在庫を減算。g/ml単位は実量ぶん減算、それ以外の単位（個・枚等）は1つ消費した扱いで-1
- 在庫が0件のときはこのUIごと非表示（無駄なプロンプト肥大化も防止）
- **既知の落とし穴（2026-09-15、下記の再設計で解消）**: 当初のプロンプトは「写っている食品を識別」としか指示しておらず、AIが「麻婆豆腐」のように完成料理名1つにまとめて返すか、豆腐・ねぎ・ひき肉…と食材ごとに分解して返すかが写真次第でブレていた。完成料理名1つにまとまると、パントリー在庫（生の食材単位）と一致しようがなく紐づけ・減算が効かなくなる。一度は「食材レベルまで必ず分解する」指示に倒したが、それだと今度は**食事記録が「豚肉」「キャベツ」…と食材の羅列になり、料理名で見たいというユーザー体験を損なう**というトレードオフが判明した。

### 二層構造（料理名 + 材料）への再設計（2026-09-15）

ユーザー提案: 「食事記録には料理名を出したい。そのうえで自炊か外食かを聞き、自炊なら使った材料を出してパントリーから減らすか確認、間違っていれば材料の追加・修正もできるように」という設計案を採用。プロンプト・データ構造・UIを全面的に二層化：

- **AIのJSON応答を2階層に変更**: トップレベルは「料理」単位（`name`=料理名、`estimatedGrams`/`kcal`等=料理全体の栄養価）。各料理の中に`ingredients`配列（`name`/`estimatedGrams`/`matchedPantryId`）をネストし、食材レベルのマッチングはここだけで行う。**食事記録（`state.meals`）に積まれるのは料理単位の1エントリのみ**で、料理名がそのまま表示される
- UIも合わせて2階層に: 各料理カードの下に「🍳自炊／🍱外食・出来合い」のトグル（`.pill-tabs`流用）を追加。AIが材料を返した料理はデフォルトで「自炊」、材料が空だった料理はデフォルトで「外食」を選択状態にする（ヒューリスティック）
- 「自炊」を選んだときだけ、材料の編集可能な一覧（`.ingredient-panel`）を表示。各行は「使う/使わないチェック」「材料名（テキスト編集可）」「量(g)（編集可）」「パントリー紐づけセレクト」「×削除」。「＋材料を追加」ボタンで空行を追加できる（AIが見落とした材料を手動補完する用途）
- **実装上のポイント**: トグルの切替・材料行の追加/削除は、`renderMeals()`を呼ばず**素のDOM操作**（`classList.toggle`・`hidden`プロパティ切替・`insertAdjacentHTML`・`.remove()`）で完結させている。理由は、既存の全再描画パターン（`state`を書き換えて`renderMeals()`で丸ごと作り直す）だと、他の行で入力中の値やフォーカスが失われるため。委譲リスナーは`#panel-meals`という永続要素に対して、上記「再描画のたびにイベントリスナーが重複登録される」と同じ`dataset`ガード（`el.dataset.ingredientUiBound`）で1回だけバインド
- 「選択した項目を追加」時、各料理行について: (1) 料理単位で1件`state.meals`にpush、(2) 自炊トグルがONの行だけ`.ingredient-row`をDOMから直接読み取り、チェック済みかつパントリー紐づけが選ばれている行だけ`deductPantryForConsumption()`で減算。材料名テキストは表示・手動編集用のラベルとして使うのみで、`state.meals`には保存されない（保存されるのは料理名の方）
- **初回リリース時のバグ（同日中に修正）**: 当初は`state.pantry.length===0`のとき自炊/外食トグルと材料一覧セクションごと非表示にしていた。「紐づけ先が無いなら出す意味がない」という判断だったが、パントリーが空の状態でテストしたユーザーから「機能が消えた」と誤解される結果になった。→ パントリーが空でも常にトグル・材料一覧は表示し、空のときは「パントリーが空のため今は在庫と紐づけられません」という案内文に差し替える形に修正。在庫との紐づけセレクト自体は`state.pantry.length`が0のときは（選択肢が無いので）引き続き非表示

### 食事記録一覧の拡張（2026-09-15）

ユーザー提案で3点追加：
- **自炊/外食の記載**: 食事記録の各行に`state.meals[].cookMode`（`"home"`/`"out"`/未設定）を追加し、設定されていれば「🍳 自炊」「🍱 外食・出来合い」のタグを表示。写真解析での自炊/外食トグルの選択結果がそのまま入る。ホーム画面の「これを記録する」（パントリー食材を使った提案）も自動的に`"home"`。手動フォーム（食品ドロップダウン・カスタム入力）は自炊/外食の概念が薄いため`cookMode`は付与せず、その場合タグ自体を表示しない
- **料理名クリックで材料詳細**: `cookMode==="home"`かつ材料が記録されている行だけ、`<details><summary>`で料理名自体をクリック可能にし、展開すると使った材料（名前・g）の一覧が出る。材料は`state.meals[].ingredients`（`[{name,estimatedGrams}]`）に保存——写真解析時に「使う」にチェックが入っていた行をそのまま保存したもので、パントリー紐づけの有無に関わらず記録する（紐づけ無しでも「何を使ったか」の記録として残したいため）
- **食事タブごとの栄養バランス表示**: 「本日合計」（全食事タブ合計・従来通りテキストのみ）とは別に、選択中の食事タブ（朝食/昼食など）だけの合計を`renderHome()`と同じ`.meters`（トラックバー）UIで表示する`mealTypeMeters()`を追加。ただし1食分は1日の目標よりずっと少ないのが当たり前なので、`nutrientStatus()`による好調/不調バッジは使わず、「1日の目標のXX%」という中立的な表示（新設した`.badge.neutral`）に留めている

## AIアイコン推測（2026-09-15追加）

バーコードスキャン・カスタム入力で登録した食材（FOOD_DBに一致が無いためカテゴリ由来の汎用アイコンしか出せなかったもの）に、Geminiで食品名からふさわしい絵文字を1つ推測させる機能。「画像を推測」という元の要望に対しては、このアプリのUIが絵文字ベース（`CAT_ICON`）で統一されているため、実写真の生成ではなく絵文字1文字の推測という形にスコープを絞った。

- `guessFoodIcon(name)`: テキストのみのGemini呼び出し（画像添付なし）。「絵文字を1つだけ返せ、説明や記号は付けるな」と指示し、返ってきたテキストの1語目を候補として採用。半角英数字を含む・8文字を超える場合は「説明文が混ざった」とみなして破棄（純粋な絵文字1つだけを信頼する簡易バリデーション）
- パントリー登録（`pantryForm`のsubmit）で、カスタム食材（`foodKey`が無い＝FOOD_DB非一致）を新規追加/マージした直後に**非同期で**（追加自体をブロックしない・Promiseで裏側から）呼び出し、結果が返ったら該当アイテムの`icon`フィールドを設定して再描画。APIキー未設定・失敗時は何も起きず、従来通りカテゴリ由来の絵文字にフォールバックする（サイレント degradation）
- `rowItemHtml()`のアイコン解決順序を `p.icon || CAT_ICON[p.category] || "🍽️"` に変更（個別推測 > カテゴリ既定 > 汎用既定）
- 既存のGemini利用状況ログ（`logGeminiUsage`）にもこの呼び出しを記録するので、設定タブの「本日の呼び出し」件数にはアイコン推測分も含まれる

## Geminiリクエストの共通化（`callGeminiText`、2026-09-15リファクタ）

AIアイコン推測を追加した時点で、fetch＋503リトライ＋25秒タイムアウト＋エラーコード判定＋利用ログ記録、という同じロジックが3箇所（写真解析・アイコン推測・下記のAI献立提案）にコピペされそうになったため、`callGeminiText(promptText, opts)` という共通関数に切り出した。

- `opts.image`（`{mimeType,data}`）を渡すと画像付き、渡さなければテキストのみのリクエストになる
- `opts.maxOutputTokens` / `opts.timeoutMs` / `opts.noRetry`（アイコン推測のような軽量リクエストでは503リトライ不要なので1回で打ち切れる）で挙動を調整
- 戻り値は `{ok:true, text}` または `{ok:false, code}`（`code`は`PHOTO_ERROR_MESSAGES`のキーと共通）。呼び出し側はエラーメッセージのマッピング（`PHOTO_ERROR_MESSAGES[code]`）だけ書けばよく、fetch自体の詳細を知らなくてよい
- `runPhotoAnalysis`・`guessFoodIcon`・`runAiSuggestions`は全てこの関数経由。個別に503リトライやタイムアウトの実装を持たない
- JSON配列のパース（```コードフェンス除去＋`JSON.parse`）も`parseGeminiJsonArray(text)`として共通化し、`geminiPhotoItemsFromResponseText`から利用

## AI献立提案（ホーム、2026-09-15追加）

「今は材料ごとで料理を提案しているね。そうじゃなくて、今使うべき食材を使って組み合わせた料理をAIで複数提案してほしい。料理名をクリックしたら必要な材料と不足状況を教えてほしい」というユーザー提案で実装。

- **呼び出しタイミングはユーザー明示確認済みでボタン押下時のみ**（ホームタブを開く/切り替えるたびに自動実行すると無駄にAPI枠を消費するため、ユーザーに確認のうえボタン方式に決定）。`aiSuggestUi`状態（`idle`/`loading`/`done`/`error`）で管理
- Geminiにパントリー全件（id・名前・数量・単位・`daysUntilExpiry`＝期限までの日数）を渡し、「期限が近い食材を優先して使う具体的な料理を最大4つ、各料理の材料と、その材料が在庫にあるか(`matchedPantryId`)を含めて返せ」と指示。**JSONスキーマは写真解析のdish+ingredients構造と意図的に同一にし**、`geminiPhotoItemsFromResponseText`をそのまま再利用（`reason`フィールドだけ新設し、写真解析側では単に使われず無視される）
- UIは料理名を`<details><summary>`にして、押すと材料一覧＋在庫状況（「在庫あり」／「不足」の色分けタグ）を展開表示。常時表示は料理名とAIが挙げた理由（`reason`）のみ
- 各提案に「これを記録する」ボタンを追加。押すと(1)料理を1件`state.meals`に記録（`cookMode:"home"`、材料は`ingredients`としてそのまま保存し記録一覧からも詳細を見られる）、(2)在庫にある材料（`matchedPantryId`が付いたもの）だけ`deductPantryForConsumption()`で減算。写真解析・食事記録の実装と完全に同じ関数・同じデータ形を再利用している
- Gemini APIキー未設定時は、このAI提案カードの代わりに従来のヒューリスティック提案（`suggestionList()`、材料の役割タグ付けによる組み合わせロジック）にフォールバックする。パントリーが0件のときは「まず在庫を登録してください」とエラー表示してAPIを呼ばない

### お気に入り献立の保存（2026-09-18追加）

AI提案・食事記録それぞれの「これを記録する」ボタンは中身が全く同じ処理（栄養価計算→在庫にある材料を減算→`state.meals`にpush）だったため、共通関数`logDishAsMeal(d)`に切り出した。

- 各AI提案カードに「☆ お気に入り」ボタンを追加。押すと`state.favoriteDishes`（最大20件、名前が同じものは重複させずトグル）に保存される
- **画像（`imageDataUrl`）は保存時に意図的に取り除く**。`aiSuggestUi.dishes`はページ再読み込みで消えるセッション内メモリだけの状態なので大きなbase64画像を持っていても問題ないが、`favoriteDishes`は`localStorage`に永続化されるため、画像を含めると際限なく容量を圧迫しかねないため
- ホーム画面に「⭐ お気に入りの献立」カードが（1件以上あるときだけ）追加され、各項目に「これを記録する」（`logDishAsMeal`を再利用）と「削除」ボタンが付く。AIを呼ばずに済むので無料・高速

### レシピ表示（Google検索連携、2026-09-19追加）

「タップした料理のレシピも見たい。できれば実在のレシピサイト（クラシル等）から、無ければAIが作成、ただし出典を明記」というユーザー要望。

- `callGeminiText`に`opts.tools`を追加し、任意のツール定義（`[{googleSearch:{}}]`）をリクエストにそのまま渡せるようにした。あわせて戻り値に`groundingMetadata`（検索結果の引用元情報）を追加
- **`generateContent`エンドポイントでもGoogle検索連携（`googleSearch`ツール）が使えることを実機で確認済み**。Gemini APIには新しい「Interactions API」向けの検索連携ドキュメントしか見当たらなかったが、無効なAPIキーで実際にリクエストを送り、「フィールド名不明」エラーではなく「APIキー無効」エラーが返ってくることを確認する方法で、`googleSearch`が`generateContent`のToolスキーマ上も有効なフィールドであることを検証した（このプロジェクトは意図的に`generateContent`だけを使っているため重要な確認だった）
- `fetchRecipeForDish(source, idx)`: 「「〇〇」という料理の作り方の手順を、可能であればクラシル等の実在レシピサイトをWeb検索して参照し、無ければ一般的な知識で」という日本語プロンプトを`googleSearch`ツール付きで送信し、`{"steps":[...]}`のJSONを受け取る
- **出典の判定はAIの自己申告を信用しない**。モデルに「検索したか」を書かせるのではなく、レスポンスの`groundingMetadata.groundingChunks`（Google側が実際の検索実行時にのみ構造的に埋める、モデルの自由記述とは別のフィールド）にWebソースがあるかどうかだけで`source.type`を`"web"`（実在サイトへのリンク付き）か`"ai_generated"`（AI作成の注記）に振り分ける。ハルシネーション対策として、モデルの主張よりGoogle側の構造化フィールドを信頼する設計
- AI提案カード・お気に入りカードの両方に「📖 レシピを見る」ボタンを追加（共通の`recipeButtonHtml`/`recipeDisplayHtml`ヘルパー）。手順は番号付きリストで表示し、直下に出典（実サイトへのリンク、またはAI作成の注記）を必ず表示する
- 画像生成と同じく**完全に手動トリガーのみ**。お気に入り保存時、レシピが「検索中」のまま保存されると解決されずに固まるため、その場合は保存時にレシピごと破棄する（画像と違い、レシピ自体はテキストで軽量なので取得済みなら保存時に保持する）
- 材料の`matchedPantryId`は保存時点のパントリー状態のスナップショットなので、時間が経って該当の在庫が無くなっていた場合は単に減算がスキップされるだけ（`deductPantryForConsumption`呼び出し前の`if(stock)`チェックが既存の安全弁として機能する）
- **検索付き呼び出しが429で失敗する問題への対応（2026-09-19、同日中に追加）**: 「今夜のおすすめ（検索無し）はできるのにレシピ（検索付き）だけ429になるのはなぜ」とユーザーから質問。調査の結果、**Google検索連携（`googleSearch`ツール）はテキスト生成そのものとは別に、専用のより小さいクォータ（月あたり数千件程度、Gemini 3.x系モデル共有）を持つ**ことが判明——テキスト生成の枠がまだ残っていても、検索連携の枠だけ先に尽きることがある。ユーザーは「課金は厳禁、無料のAIは無いのか」と明言（Claudeのweb_search等、他の有料APIへの切替は却下）。そこで**同じGeminiキー・同じ無料枠のまま**、検索付き呼び出しが`rate_limited`または`server_overloaded`で失敗した場合だけ、同じ質問を`tools`無し（検索無し）で自動的に再送する形に変更。検索無しの再送が成功すれば、既存の判定ロジック（`groundingMetadata`の有無）により自動的に`source.type:"ai_generated"`として正直に表示される（`invalid_key`など検索の有無に関係なく失敗する種類のエラーは再送せず、そのままエラー表示にする——直しようがない失敗にリクエストを2倍消費させないため）

### 献立の画像生成（2026-09-18追加、当日中にNano Banana→Pollinations.aiへ変更）

「Gemini画像生成モデルで、おすすめ夕食を画像にする」というユーザー要望。設定タブでON/OFFを選べるようにし、デフォルトはOFF。

- **初回実装**: `gemini-3.1-flash-image`（Nano Banana）を使用。実機で試したところ`429 RESOURCE_EXHAUSTED`・`limit: 0`で失敗。原因を調べると、この画像生成モデルはテキスト系モデルと違って**無料枠が存在せず**、Google側で請求先（課金）を有効化しないと`limit: 0`のまま一切使えないことが判明（1枚あたり$0.045〜$0.15の完全従量課金）
- ユーザーに確認したところ課金はせず、代わりに無料の代替を探すことになった。調査の結果、**Pollinations.ai**（`image.pollinations.ai`）という、APIキー・サインアップ一切不要でCORSも許可された無料画像生成サービスを発見。実機で2回テストし、料理写真として十分実用的な画質を確認（Artifactでユーザーに実物を見せて承認を得た）
  - 実装は`GET https://image.pollinations.ai/prompt/{URLエンコードしたプロンプト}?width=768&height=768&nologo=true&model=flux&referrer=nutristock`を`fetch`するだけ。レスポンスは画像バイナリそのもの（JSON経由ではない）なので、`blob()`→`FileReader.readAsDataURL()`でdata URLに変換
  - **`model=flux`を明示するのが重要**: 既定モデル（`sana`）は共有の混雑時に`429`（コミュニティ全体のレート制限）で失敗することを実機で確認したが、`model=flux`を指定すると安定して成功した
  - `nologo=true`を付けても右下に小さく「pollinations.ai」の透かしロゴが残る（除去する公開パラメータは見つからず）。個人用途としては許容範囲と判断
  - Geminiと違い**APIキー不要**なので、この機能はもう`hasGeminiKey()`に依存しない。Gemini利用状況ログにも記録されない（別サービスのため）
- 既存の`callGeminiText`とは別に`generateDishImageDataUrl(promptText)`のまま維持（中身だけPollinations.ai向けに差し替え）。503相当（コミュニティのレート制限）を見越して1.5秒待って1回だけ自動リトライする、既存のGemini呼び出しと同じ設計哲学を踏襲
- 設定タブの「AI設定」カードに「🎨 今夜のおすすめに画像生成ボタンを表示する」チェックボックスを追加（`imageGenEnabled`、`localStorage`に保存）。ONのときだけ各AI提案カードに「🎨 画像を生成」ボタンが現れる
- **完全に手動トリガーのみ**（提案が出たときに自動生成することは一切しない）。ボタンを押した1件だけを生成する。生成された画像は`aiSuggestUi.dishes[idx].imageDataUrl`に保持され、その場で再描画されてカードに表示される。お気に入り保存時は上記の通り画像を破棄する
- **プロンプトは日本語ではなく英語で（2026-09-18、当日中に修正）**: 当初「日本の家庭料理「〇〇」の...」という日本語プロンプトにしていたところ、実機で生成結果が「かなりひどい」と報告された。Pollinations.aiが使うFluxモデルは英語キャプション中心で学習されており日本語指示への追従が弱いことが原因と判断し、料理名（固有名詞）だけ日本語のまま埋め込みつつ、指示文自体は英語に変更。実機テストで写真としての完成度は改善したが、麻婆豆腐のような特定の日本料理の見た目の再現度には無料モデルなりの限界がある
- **料理名だけでは不十分と判明・Geminiに英語の見た目説明を書かせる方式に変更（同日中に再修正）**: ユーザーから「AI提案のどの料理を画像化してもほぼ同じ、意味不明な画像になる」と報告。実機で「肉じゃが」と「オムライス」を比較したところ、構図・色味がほぼ同一の生成物になることを確認——Fluxが日本語の料理名をほとんど無視し、共通の英語定型文だけを手がかりに生成していたのが原因（`seed`未指定だと同一プロンプトに対して常にバイト単位で同一の画像が返る＝決定論的なことも実機で確認済み。「🎨 再生成」ボタンが実質何も変えていなかったバグでもあった→`seed`にランダム値を付与して修正）。
  - 根本対策として、AI献立提案のJSONスキーマに`imagePromptEn`（この料理の見た目を英語で説明する一文。色・主な材料・盛り付け・器などを英語だけで具体的に）フィールドを追加。日本語が得意なGemini側に見た目の英訳を書かせ、それをそのままFluxへのプロンプトに使う（`"a bowl of simmered beef and potatoes in sweet soy sauce, glossy brown sauce..."`のような具体的な英語記述）ことで、実機テストで生成結果が実際にその料理らしく見えるようになることを確認
  - `imagePromptEn`が無い場合（`imagePromptEn`フィールド追加前に保存された古いお気に入り献立など）は、従来の「料理名を埋め込むだけ」のテンプレートにフォールバックする
- **材料と画像の不一致を防ぐ調整（同日中、ユーザー指摘で再修正）**: 「材料リストと画像が食い違っていたらおかしい」という指摘を受け、JSONスキーマの**`ingredients`を`imagePromptEn`より先に書かせる順番に変更**した（LLMは前から順に生成するため、材料を確定させてから見た目を説明させる方が内容が食い違いにくい）。あわせてプロンプトに「`imagePromptEn`は直前の`ingredients`で列挙した材料の色・形・切り方を明示し、そこに無い食材を想像で足さないこと」と明記

## 在庫切れ間近の自動リマインド＋パントリー健康度スコア（2026-09-18追加）

ネットの類似アプリ調査（Eatvora等）からアイデアを得て実装。

- **`addToShoppingListIfLow(item)`**: 個数系の単位（g/ml以外＝個・パック・本・枚など）の在庫が残り1個になった時点で、買い物リストへ自動追加する。g/mlは「残りわずか」の閾値を自然に定義できないため対象外。呼び出し元は`deductPantryForConsumption()`（食事記録・AI写真解析・AI献立提案の全消費経路で共通）と、パントリータブの「－1」ボタン。以前は「在庫が0になった瞬間」にしか買い物リストへ追加しなかったが、これで「残り1個」の時点で早めに気づけるようになった
- **パントリー健康度スコア（`pantryHealthScore()`）**: AI不要、クライアント側の単純計算のみ。60点満点＝鮮度（期限切れ・期限間近の食材が少ないほど高い）、40点満点＝栄養バランス（肉魚卵大豆・野菜・穀物の3カテゴリが揃っているか）の合成。ホーム画面の統計タイル欄に追加し、良好(80+)/普通(50-79)/要改善(<50)のバッジと、期限切れ件数や不足カテゴリを表示する

## ホーム画面アイコン（PWA体裁、2026-09-18追加）

「ホーム画面に追加」した際のアイコンが未設定（ページのスクリーンショットが使われる）だったのを解消。画像編集ツールが実行環境に無かったため、Node.jsの標準`zlib`だけでPNGファイルを直接バイト列から生成するスクリプトを書いて対応した（アクセント色の正方形に白い円、という簡素なプレースホルダーアイコン）。

- 生成ファイル: `apple-touch-icon.png`(180x180)・`icon-192.png`・`icon-512.png`
- `manifest.json`を新規作成（`name`/`short_name`/`display:standalone`/`theme_color`など）し、`<head>`に`<link rel="manifest">`・`<link rel="apple-touch-icon">`・`<meta name="theme-color">`等を追加
- アイコンの見た目自体は簡易的な仮デザインなので、後でちゃんとしたロゴに差し替える余地あり

## バーコード→Open Food Facts栄養情報（2026-09-18追加）

「ネットの類似アプリ調査」から出たアイデア①。Open Food Facts（`world.openfoodfacts.org`）は無料・APIキー不要・CORS全開放（`Access-Control-Allow-Origin: *`、実機で確認済み）の商品データベース。バーコードから商品名だけでなく栄養価まで自動取得できるようにした。

- **栄養素のマッピング方針（ユーザー指示）**: NutriStock独自の栄養素定義に変換しようとせず、**Open Food Facts側のフィールドをそのまま使う**。幸い`energy-kcal_100g`/`proteins_100g`/`fat_100g`/`carbohydrates_100g`/`fiber_100g`/`salt_100g`という100gあたりの生フィールドが、単位（kcal/g）も意味も含めてNutriStock側の`kcal/p/f/c/fiber/salt`とほぼ1:1で対応しているため、変換ロジックは不要だった（実際のAPIレスポンスで確認済み）
- `lookupOpenFoodFacts(code)`: `GET /api/v2/product/{code}.json?fields=product_name,product_name_ja,nutriments`。`status===1`で見つかった場合のみ有効。商品名は`product_name_ja`があればそちらを優先、無ければ`product_name`
- **呼び出しタイミング**: バーコードをスキャンして、①ローカルに学習済みでも②パントリーに在庫があるのでもない、**完全に初めて見るバーコードのときだけ**呼び出す（無駄なリクエストを避ける）。ヒットすれば商品名と栄養情報を自動入力、外れれば従来通り手入力を促す（サイレントにフォールバック）
- 取得した栄養情報は`state.barcodes[code].nutrition100`として学習済みバーコード情報に一緒に保存され、パントリーアイテム自身にも`p.nutrition100`として乗る。次回同じバーコードをスキャンしたときは再取得せずキャッシュを使う
- **カバレッジの限界**: crowdsourceのため日本の商品網羅率は欧米ほど高くない見込み。「当たればラッキー」という位置づけで、外れても既存の手入力フローに何の支障もなく落ちる
- **食事記録への統合**: `nutrition100`を持つパントリーアイテムは、食事記録タブの食品選択ドロップダウン（`mealFoodOptionsHtml()`、新設）に「在庫の商品（栄養情報あり）」というoptgroupとして追加され、選択すると100gあたりの値から実量ぶんの栄養価を計算する。選択時に「在庫があれば自動で減らす」を使うと、`deductPantryForConsumption()`でパントリーも減る（食品データベース由来の食材と全く同じ体験）

## レシート一括登録（2026-09-18追加）

パントリータブに「🧾 レシートから一括登録」カードを追加。写真解析（`runPhotoAnalysis`）とほぼ同じ構造で、レシート写真1枚から最大20件の食品を抽出し、カテゴリ・数量・単位・賞味期限の目安を推定させる。

- プロンプトで「税金・割引・ポイント・レジ袋等の非食品行は無視」と明示的に指示
- AIが返す`expiryDays`（賞味期限までの目安日数、整数）から`today + N日`を計算してdate inputに初期値として入れる。すべて編集可能で、日付ピッカーで直接修正できる
- カテゴリ・単位はAIの返り値を`CAT_LABEL`・許可単位リストで検証し、不正な値なら`other`/`個`にフォールバック（ハルシネーション対策、写真解析の`matchedPantryId`検証と同じ考え方）
- 「選択した項目をパントリーに追加」時、`pantryForm`のsubmitハンドラと同じ「同名＋同じ賞味期限ならマージ」ロジックを適用し、新規追加した項目にはバーコード/カスタム入力と同様に`guessFoodIcon()`でアイコンを非同期取得する

## 音声で食事記録（2026-09-18追加）

食事記録タブの「写真から記録」カードに「🎤 音声で記録」ボタンを追加。写真解析`runPhotoAnalysis`と全く同じJSONスキーマ（料理名+栄養価+ingredients）・全く同じ`photoUi`状態・全く同じレビューUIを共有し、入力元が画像でなく音声認識のテキストになっただけの実装（`runVoiceAnalysis(transcript)`）。

- `window.SpeechRecognition || window.webkitSpeechRecognition`でブラウザ対応を判定し、非対応ブラウザではボタン自体を表示しない。iOS Safariは14.5以降`webkitSpeechRecognition`に対応済み（ただしcontinuousモード・interimResultsまわりに既知の不具合があるとされるため、`continuous:false`・`interimResults:false`の一発認識のみを使い、それらの不具合を踏まない設計にした）
- 認識中はボタンのテキストを直接DOM操作で「🎙️ 聞き取り中…」に変更（`renderMeals()`を挟まない — 再描画するとボタン自体が作り直されてしまうため）。認識結果が返ってきた時点で`runVoiceAnalysis`が呼ばれ、以降は写真解析と同じ流れ（ローディング表示→dish+ingredientsのレビューUI）になる

## 体重ログ（2026-09-18追加）

ネットリサーチで出てきた「adaptive macro targeting（体重の実測推移に応じて目標を動的に再調整する）」という考え方に着想。ただし他社アプリが使っている具体的な調整アルゴリズムは検証できないため、**独自の調整式を発明することはせず**、次の誠実な範囲に留めた：

- 設定タブに「体重ログ」カードを追加（プロフィールカードと栄養目標カードの間）。`state.weights`（日付→体重kgのマップ、`state.steps`と同じ構造）に記録
- 記録すると、`state.profile.weight`もその値に更新し、既存の`calcTargets()`をそのまま呼び直して目標を再計算する——つまり「アプリが今の体重を常に正しく知っている状態を保つ」という意味での自動化であり、体重の増減トレンドから独自に目標を上下させるような予測的ロジックは実装していない
- `weightTrend()`は直近7日以上前の記録と最新記録の差分を計算して表示するだけの**情報提供**（例:「直近7日間で -0.4kg」）。目標値の計算には一切使わない。ユーザー自身がこの数字を見て、必要なら「現在の栄養目標」カードから手動で調整する、という位置づけ

## 歩数の自動入力（iOSショートカット連携、2026-09-15追加）

HealthKitはWebページから触れないため完全自動は不可能だが、「タップ1回で今日の歩数をヘルスケアから読み取ってNutriStockに反映する」という準自動の橋渡しを実装した。バックエンドを持たない構成なので、URLのクエリパラメータ経由でページ自身のJSに読み取らせる方式。

- `index.html`に`applyStepsFromUrl()`を追加。ページ読み込み時（`initDb()`の直後、`state`のロード完了後）に`location.search`の`steps`パラメータを見て、あれば`state.steps[今日の日付]`に設定・保存し、URLからパラメータを消してから（`history.replaceState`）再描画する。既存の歩数プリセットボタン・手入力フォームと全く同じ「その日の合計歩数を上書き保存」という扱いなので、何度実行しても安全（冪等）
- **iOS「ショートカット」アプリでの設定手順**（ユーザー側で1回だけ設定が必要）:
  1. ショートカットを新規作成
  2. アクション「ヘルスケアのサンプルを検索」（または「歩数を取得」相当のアクション）を追加し、種類＝歩数、期間＝今日、集計＝合計、に設定
  3. アクション「URLを開く」を追加し、URLに `https://kaeru510.github.io/nutristock/?steps=` の末尾に手順2の結果（歩数の変数）を連結したものを指定
  4. 名前を付けて保存し、共有ボタンから「ホーム画面に追加」しておくと、ホーム画面のアイコンをタップするだけで歩数が自動反映されたNutriStockが開く
- 完全放置の時間指定自動実行（パーソナルオートメーション）は、Safari表示を伴わない「サイレント実行」にすると単なる静的ファイルへのHTTPリクエストになってしまい、バックエンドが無い今の構成では何も保存できないため不可。あくまで**タップ（またはSiriへの呼びかけ）起点**の運用になる
- **サイト内から直接起動するボタンを追加（2026-09-15）**: ホーム画面に置いたショートカットのアイコンを別途タップしに行く手間を省くため、ホームタブの歩数カードに「📲 ヘルスケアから今日の歩数を取得」ボタンを追加。iOSの`shortcuts://run-shortcut?name=<ショートカット名>`というURLスキームをリンク先にすることで、ボタン1つでSafari→ショートカットアプリ起動→歩数取得→`?steps=`付きURLでSafariに戻る、という一連の流れをページ内から開始できる
  - ショートカット名はページ内に`STEPS_SHORTCUT_NAME`という定数でハードコードしており（現在「歩数をNutriStockに送る」）、URLスキームはショートカット名の完全一致が必要なため、ユーザーが端末側でショートカット名を変更した場合はこの定数も合わせて変更する必要がある
  - `IS_IOS`（UserAgentで`iPad|iPhone|iPod`判定）でiOS端末のときだけボタンを表示。他の端末ではURLスキームが存在しないため無意味なボタンを出さない
  - **ハマったポイント1**: ショートカット側で、文字列（`https://.../?steps=`）と数値の変数（合計アクションの結果）を1つの入力欄で直接連結すると「リッチテキストをURLに変換できなかったため失敗」というエラーになった。ユーザーと一緒にデバッグし、間に単純な「テキスト」アクションを1つ挟んで一度プレーンテキストとして連結してから「URLを開く」に渡す、という回避策で解決（Shortcuts側の既知の癖）
  - **ハマったポイント2（タブの重複）**: `shortcuts://run-shortcut`→ショートカット実行→「URLを開く」でSafariに戻る、という往復を毎回行うため、Safari側は「外部アプリから新規に開けと言われた」扱いになり、実行するたびにタブが増え続ける。ページ側のコードでは制御不可能なSafari/Shortcutsの仕様。「ホーム画面に追加してWebアプリ化する」という回避策も試したが、ショートカットの「URLを開く」自体が常に通常のSafariを指定するため効果なし（Webクリップは`Appを開く`アクション等の対象にもならず、外部から直接起動する手段が無い）。最終的に、ショートカット内で**新しいタブを開く前に、URLに`nutristock`を含む既存タブを検索して閉じる**という2アクション（「次に当てはまるタブを検索」→「タブを閉じる」、フィルタ条件は「が次と等しい」ではなく必ず「**が次を含む**」にすること。`?steps=`の数値は毎回変わるため）を先頭に追加することで解決。ユーザーが誤って別の独立したショートカット（共有シート経由で動く「Close Current Tab」）を作ってしまい「反応しない」と混乱した一幕もあったが、最終的に同一ショートカット内に統合して解決した
- **手入力UIの廃止（2026-09-16）**: ショートカット連携が安定して動くようになったため、ユーザーの判断で歩数プリセットボタン（3000/7000/12000歩）と手入力フォーム（`stepsForm`/`stepsInput`）を完全に削除。今は「📲 ヘルスケアから今日の歩数を取得」ボタン（iOS限定）のみが歩数の入力手段。iOS以外の端末やショートカット未設定時は歩数を記録する手段が無くなる点は、ユーザーに一度確認したうえで了承を得ている

## 期限切れ通知（iOSショートカット連携、2026-09-18追加）

歩数連携と全く同じ「時間指定オートメーション→URLを開く→ページ側のJSが処理」のパターンを、期限通知にも適用。**こちらはヘルスケアデータを扱わない分、歩数連携よりショートカット側の設定がずっと簡単**（「ヘルスケアサンプルを検索」「統計を計算」「テキスト」の3アクション構成が不要で、「URLを開く」1アクションだけで済む）。

- 設定タブに「期限切れ通知」カードを追加。「通知を有効にする」チェックボックスと「何日前から通知するか」（デフォルト2日）を`state.notifySettings = {enabled, daysBefore}`に保存
- `checkExpiringItems()`: `daysUntil(expiry) <= daysBefore`の食材（期限切れ含む）を期限が近い順に返す
- `applyExpiryCheckFromUrl()`: ページ読み込み時に`location.search`の`checkExpiry`パラメータを見て、通知が有効なら該当食材リストを組み立て、`Notification` API（未許可なら`requestPermission()`を挟む）でOS通知を出す。通知が出せない/拒否されている場合でも、Safariはショートカットによって前面に来るため、最低限トーストで件数を表示する
- **iOS「ショートカット」アプリでの設定手順**:
  1. 新規オートメーションを作成し、トリガーを「時刻」で希望の時間（例：毎朝8:00）に設定
  2. アクション「URLを開く」を1つだけ追加し、URLに `https://kaeru510.github.io/nutristock/?checkExpiry=1` を指定
  3. 保存時に「実行前に確認」をOFFにしておくと、時刻になったときに確認タップ無しで自動実行される（ONのままだと通知バナーをタップするまで実行されない）
  4. 歩数連携と同様、タブが積み重なるのが気になる場合は、「URLを開く」の前に「次に当てはまるタブを検索（URLが次を含む: nutristock）」→「タブを閉じる」の2アクションを追加すると1タブに保たれる（歩数連携のショートカットで実証済みの回避策と同じ）
- 完全なサイレント実行（Safariを一切表示しない）は不可能な点は歩数連携と同じ。オートメーションが発火すると、その時刻に一瞬Safariが前面に出る

## プラットフォーム側の制約（コードでは解決不可）

### 1. Apple ヘルスケア連携
HealthKitはネイティブiOSアプリ専用APIで、Webページから使う公式手段が存在しない。**完全に自動・バックグラウンドでの同期は不可能**（ネイティブアプリ化以外に道がない）。

ただし2026-09-15、「タップ1回で歩数だけ自動入力」という準自動の妥協案を採用・実装した。詳細は下記「歩数の自動入力（iOSショートカット連携）」。

### 2. スマホでバーコードのカメラが起動しない
エラー: `NotAllowedError` / `Permissions policy violation: camera is not allowed in this document`
- Artifactはclaude.aiのページ内にiframeとして埋め込まれる仕組み
- iframe内でカメラ(getUserMedia)を使うには、親ページ側が `allow="camera"` を明示的に渡す必要がある
- デスクトップブラウザでは動く／モバイルでは（このiframeの許可設定により）ブロックされる
- ページ側のコードでは解決不可。Anthropicへの報告事項

## バーコードスキャン実装メモ

ライブラリ: ZXing-js (`@zxing/library@0.20.0`, jsdelivr CDN経由で動的読み込み)。同ライブラリを使う参考サイト（tools.cureco.co.jp/tools/barcode-reader/）のソースを直接読んで比較検証した。

- **解像度を明示指定しない**: 当初 `width:1920,height:1080` を要求していたが、1コマの処理が重くなり実質的なフレームレートが下がって逆効果だった。指定を外し、ブラウザのデフォルト解像度に任せる。
- **カメラは `decodeFromVideoDevice(deviceId, ...)` で選ぶ**: `listVideoInputDevices()` でカメラ一覧を取得し、ラベルが `/back|rear|environment/i` にマッチするものを明示的に選択。`facingMode:"environment"` のような緩い指定より確実。ラベルは初回パーミッション許可前は空になりうるので、マッチしなければ `deviceId` を渡さず内部のfacingMode fallbackに任せる（`devices[0]` を安易に使わない — フロントカメラを掴む恐れがあるため）。
- **対応フォーマットはEAN-13のみに絞る**: 当初EAN-8/UPC-A/UPC-E/CODE-128も許可していたが、いずれも誤読の原因になった。特にEAN-8は、服などでバーコードの一部が隠れて13桁コードの断片が偶然8桁として成立してしまう誤読が実機テストで確認された（チェックデジットは通ってしまう）。日本の小売商品はほぼ全てEAN-13なので、EAN-13専用にするのが最も安全。
- **「2回連続一致」で確定させる案は逆効果だった**: 誤読対策として同じ値を2フレーム連続で読めたら確定、という仕組みを一度入れたが、実機では1回の成功自体が十分レアで、2回連続はほぼ起きず、体感の反応が大幅に悪化した。EAN-13のチェックデジット自体がノイズ除去としてある程度機能するので、1回の成功で確定する方式に戻した。
- **ガイド枠を表示**: 画面中央に横長の枠（`box-shadow`で周囲を暗くする「スポットライト」CSSトリック）を出し、バーコードを水平にそこへ合わせるよう誘導。市販スキャナーアプリの定石パターン。
- **検知位置に緑の枠を描画**: `result.getResultPoints()`（開始/終了バーの座標、動画のネイティブ解像度基準）を、`object-fit:cover` によるスケール・オフセットを計算してcanvas上にマッピングし矩形を描画。認識後0.55秒（デバッグモード時は4秒）その場に留めてから閉じる。
- **斜めの角度でしか読めない現象（未解決・調査打ち切り）**: 真正面（静止した状態も含む）では一貫して失敗し、斜めにすると確実に成功する。同じZXing-jsを使う参考実装（tools.cureco.co.jp/tools/barcode-reader/）は逆に真正面でしか読めず斜めは非対応、という対照的な報告あり。実機での同一バーコード・同一照明・URLのみ切り替えての比較でも再現し、環境要因ではないことを確認済み。以下を参考実装に合わせたが、いずれも症状変わらず：
  - 解像度の明示指定をやめる
  - `decodeFromVideoDevice`＋デバイスラベルでのカメラ選択に統一
  - `aspect-ratio:4/3`＋`object-fit:cover`による映像のクロップ表示をやめ、`height:auto`のネイティブ比率表示に統一
  - 対応フォーマットの範囲を参考実装と完全一致させる（結果はEAN_13のみに絞ってフィルタ）
  コード上で確認できる差分をすべて潰しても再現したため、原因は実装ではなく端末・ブラウザ・カメラハードウェア側のより深い部分にあると判断し、調査を打ち切った。斜めだけでなく**縦向き(90度回転)でも確実に読める**ことを確認したため、案内文・ガイド枠の形状は「縦向きにしてください」という指示に変更済み(2026-09-12)。ZXingは`TRY_HARDER`有効時、初回解析失敗時に自動で90度回転させて再解析する仕組みを内蔵していることをソースコードで確認済み（それでも正面(0度)は再現しないため、正面での撮影データ自体が壊れている＝モアレ等のセンサーレベルの干渉が濃厚）。
- **カメラのdeviceIdが経年で無効化される（2026-09-18発見・修正済み）**: PCでは起動できるのにスマホでだけ「カメラを起動できませんでした」が発生し、詳細を確認したところ`OverconstrainedError: Invalid constraint`だった。原因は、初回に選んだカメラの`deviceId`を`localStorage`（`nutristock_cameraId`）に無期限キャッシュしており、Safari側の事情（OS/ブラウザアップデート、サイトデータのリセット等）で`deviceId`が変わることがあるのに、古いIDを無条件に信頼して`getUserMedia`に`{deviceId:{exact:古いID}}`として渡していたこと——一致するカメラが存在せず制約エラーになる。
  → 二重に対策：①`openScanner()`で`listVideoInputDevices()`した直後、キャッシュ済みIDが今回のカメラ一覧に実在するか検証し、無ければその場で破棄して再検出（一次対策）。②それでも`startDecoding()`側で`OverconstrainedError`を受け取った場合、キャッシュを破棄してブラウザのデフォルトカメラで1回だけ自動リトライ（保険）。どちらもキャッシュを消すだけで、次回以降は正常に再検出される。

## 実装上の技術メモ（ハマったポイント）

### db snapshotのデータは凍結（frozen）されている
`dbDoc.onSnapshot` で届く `snap.data()` は読み取り専用。`Object.assign(defaultState(), data)` のような浅いコピーでは、ネストしたオブジェクト（`steps`, `pantry` 等）が凍結されたまま使い回されてしまい、後から `state.steps[today] = x` のような書き込みをした瞬間に `TypeError: object is not extensible` で例外が発生する。
→ 必ず `JSON.parse(JSON.stringify(data))` 等でディープクローンしてから使うこと。

### 複数タブ/複数端末での上書き対策
`dbDoc.set(state)` で毎回全データを丸ごと上書きすると、別タブの古いローカルデータが最新の変更を消してしまうことがある。
→ 変更した項目だけ `dbDoc.update({変更したキー: 値})` で部分更新する（`persist(fields)` のように変更フィールドを明示する）。

### db接続前のクリックが握りつぶされる競合
ページ表示直後、`claude.use("db")` の接続が完了する前にユーザーが操作すると、その変更がローカルにしか反映されず、直後に届く「まだ書き込まれていない古いサーバーデータ」で上書きされてしまうことがある。
→ 接続未完了時の変更は `hasUnsyncedLocalChange` フラグで検知し、初回スナップショット受信時にサーバーデータで上書きせず、ローカルの変更を優先してpushする。

### 再描画のたびにイベントリスナーが重複登録される（2026-09-12発見）
`renderPantry`/`renderMeals` 内で `el.addEventListener("click", ...)` のようにパネルの**永続的なコンテナ要素**へ委譲リスナーを付けていたが、これは`renderAll()`（アプリ内のどんな変更でも毎回全タブぶん呼ばれる）のたびに実行され、同じ要素にリスナーがどんどん積み重なっていた。`el.innerHTML=...`で子要素は毎回作り直されるので中の要素（フォームなど）は問題ないが、**委譲先の親要素自体（`#panel-pantry`など）は使い回されるため、そこに付けたリスナーだけは蓄積し続ける**。
「使い切った」のような1回で完結する操作（idにマッチする要素をfilterで除去するだけ）では、何度実行されても結果が同じなので症状が出なかったが、「－1」のような**積み重なる操作**を追加した瞬間、1タップの裏で何度も実行され、一気に0以下まで減って消えるというバグとして表面化した。
→ `if(!el.dataset.xxxBound){ el.dataset.xxxBound="1"; el.addEventListener(...); }` のように、**永続的なコンテナへの委譲リスナーは初回だけ**バインドするガードが必要。今後similarなパターンを追加する時は要注意（同じ仕組みが他の場所に潜んでいないか都度確認する）。

## Artifact runtime capability の一般的な制約（今後の開発で再度当たりうる）
- `db` capability を宣言すると、そのArtifactは**組織内限定にしかできない**（一般公開不可）
- 直接開いたローカルファイル（`file://`）では `window.claude` が一切存在せず、db・sample等すべて使えない（localStorageのみのフォールバック動作になる）
- capabilityの利用可否はビューア（VS Code拡張／ブラウザ／モバイル）によって異なりうる
